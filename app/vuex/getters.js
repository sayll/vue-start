export default {
  /* searchData: state => {
    function filterData(str) {
      return state.content.ipcrsCreditReportQueryRecordsItems.filter(item => {
        return item.search_reson.indexOf(str) !== -1
      })
    }

    return {
      self: filterData('本人'),
      card: filterData('信用卡'),
      loan: filterData('贷款'),
      manage: filterData('管理'),
      assure: filterData('担保')
    }
  } */
}
